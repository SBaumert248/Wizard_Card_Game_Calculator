<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ① Wenn das Feld den Fokus hat → schwarze Border -->
    <item android:state_focused="true"
        android:drawable="@drawable/rounded_edit_focus" />

    <!-- ② Standard (kein Fokus) → transparenter Border -->
    <item android:drawable="@drawable/rounded_edit" />

</selector>